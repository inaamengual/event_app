<h2><%= @inventory_item.name %></h2>

<%= "#{@inventory_item.quantity} Available"%>
<%= "$#{@inventory_item.price}"%>
<%= "#{@inventory_item.category} "%>
<%= "#{@inventory_item.subcategory} "%>
<%= "More Info: #{@inventory_item.description}"%>

<% if current_company %>
  <%= link_to 'Edit', edit_inventory_item_path %>
<%end %>
